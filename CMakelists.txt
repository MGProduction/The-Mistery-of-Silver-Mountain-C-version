cmake_minimum_required(VERSION 3.10)
project(CSilverMountain C)

# Define the source file
set(SOURCES c_silvermountain.c)  # <-- Change 'c_silvermountain.c' to your actual file name if needed

# Create the executable
add_executable(${PROJECT_NAME} ${SOURCES})

# MSVC-specific define to disable secure warnings
if (MSVC)
    target_compile_definitions(${PROJECT_NAME} PRIVATE _CRT_SECURE_NO_WARNINGS)
endif()

# Platform-specific options
if(WIN32)
    message(STATUS "Configuring for Windows")
    # Add Windows-specific settings here if needed
elseif(APPLE)
    message(STATUS "Configuring for macOS")
    # Add macOS-specific settings here if needed
elseif(UNIX)
    message(STATUS "Configuring for Linux/Unix")
    # Add Linux-specific settings here if needed
endif()

# Optional: install target
install(TARGETS ${PROJECT_NAME} DESTINATION bin)